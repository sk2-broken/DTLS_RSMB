
To compile RSMB with DTLS 1.2 support :
sudo make broker_mqtts

Enter the following command :
./broker_mqtts -c mqtts.conf 

The RSMB broker will try to establish a bridge with a Mosquitto broker automatically
Broker IP adress is written in mqtts.conf 